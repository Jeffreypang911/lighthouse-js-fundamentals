/*function lastIndexOf (arr, num) {


for (arr.index = num;;) {
  console.log(num);

  arr.index + 1;
  }

}
console.log(lastIndexOf ([ 0, 1, 4, 1, 2 ], 6));


function lastIndexOf (arr, num)
{
  arr.forEach(function(arrayvalue, index) {
    if (arrayvalue == num)
    return (arr.length)
  }

   )

}

console.log(lastIndexOf ([ 0, 1, 4, 1, 2 ], 0)); */


function lastIndexOf (arr, num) {
  if (arr == num)

  return arr.index
}

lastIndexOf ([ 0, 1, 4, 1, 2 ], 1)
